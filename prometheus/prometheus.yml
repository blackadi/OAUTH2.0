global:
  scrape_interval: 15s
  evaluation_interval: 15s
  scrape_timeout: 10s

scrape_configs:
  - job_name: "authlete-server"
    metrics_path: "/api/metrics"
    static_configs:
      - targets: ["localhost:3000"]
        labels:
          service: "authlete-node-authz-server"
